--- 
language: php 
before_install:
addons:
  apt:
    update: true
dist: focal
sudo: required
before_install: 
  - "sudo add-apt-repository ppa:ondrej/php"
  - "sudo apt-get update"
  - "sudo apt-get upgrade"
  - "sudo apt install php8.1"
